export interface MessageOptions {
    workspaceId: string;
    apiKey: string;
    zone: "LA" | "EU";
}
export declare class Message {
    private readonly DEFAULT_MCP_URL;
    private workspaceId;
    private apiKey?;
    private mcpUrl;
    private http;
    constructor(options: MessageOptions);
    onWhatsappMessage(params: {
        from: string;
        message: string;
        sessionId: string;
    }): Promise<void>;
    onConversation(params: {
        to: string;
        templateId: string;
        variables: Record<string, string>;
    }): Promise<void>;
}
